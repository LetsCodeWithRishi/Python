# Python Basics

- `lesson.py` covers variables, built-in types, comments, input, and type conversion.
- `challenge.py` calculates a rectangle area from user input.

Run: `python 01_Basics/lesson.py`
