# Challenge: Create a program that asks for the length and width of a rectangle
# and prints the area.

length = float(input("Enter length: "))
width = float(input("Enter width: "))

area = length * width
print(f"Area of the rectangle: {area}")
