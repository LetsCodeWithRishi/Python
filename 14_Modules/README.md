# Modules

- `lesson.py` imports `math`, `random`, `datetime`, and the local helper.
- `helpers.py` provides a small reusable function.
- `challenge.py` calculates a circle area with `math`.

Run from this folder: `python lesson.py`
